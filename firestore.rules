rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Auth is handled at the application layer via JWT httpOnly cookies.
    // Firestore rules are fully open for this internal tool.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
